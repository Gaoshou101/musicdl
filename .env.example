# This sample is for the repository-based compose.yaml / compose.prod.yaml paths.
# The quick install (compose.quick.yaml) needs no .env file or external Redis;
# administrator cookies require HTTPS by default there too.
# Do not commit real credentials.
# External Redis is required by the repository-based Compose files.
# Replace the example host with your externally managed Redis endpoint.
MUSICDL_REDIS__URL=redis://redis.example.invalid:6379/0
# Optional host bind port for the main health/API endpoint.
MUSICDL_PORT=8000
# Recommended pinned image version; compose.prod.yaml falls back to latest if unset.
MUSICDL_IMAGE_TAG=1.0.3
# Default: HTTPS-only session and CSRF cookies. For direct HTTP on a trusted
# local network, set false and recreate the main service.
# The 1.0.1 images introduced this policy, and published 1.0.3 images include it;
# the original v1.0.0 images do not.
MUSICDL_ADMIN__COOKIE_SECURE=true
# Optional OpenAI-compatible advisory ranking/classification (disabled by default).
# MUSICDL_AI__ENABLED=false
# MUSICDL_AI__BASE_URL=https://api.openai.com/v1
# MUSICDL_AI__API_KEY=replace-with-a-secret
# MUSICDL_AI__MODEL=replace-with-a-compatible-model
# MUSICDL_AI__TIMEOUT=10
# MUSICDL_AI__MAX_CANDIDATES=20
# Optional: relays that only answer a specific client reject the default
# User-Agent with 401. Leave blank to use the HTTP client's own value.
# MUSICDL_AI__USER_AGENT=claude-cli/1.0.0 (external, cli)
